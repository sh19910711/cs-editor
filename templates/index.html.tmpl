<!doctype html>
<html>
	<head>
		<meta charset="utf-8">
		<title>editor</title>
	</head>
	<body>
		<h1>editor</h1>
		{{if .loginUser.LoginID}}
		<p>login as {{ .loginUser.LoginID }}</p>
		{{end}}
		<ul class="menu">
			<li><a href="/register">Register</a></li>
			<li><a href="/login">Login</a></li>
		</ul>
	</body>
</html>
